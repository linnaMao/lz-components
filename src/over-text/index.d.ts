import * as React from 'react';
interface IOverTextProps {
  data: string;
  rows?: number;
  suffix?: string;
  one?: boolean;
}
declare const OverText: React.FC<IOverTextProps>;
export default OverText;
